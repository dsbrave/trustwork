Trust loop explainer (landing page)
===================================

Expected file for The Trust Loop section:

  trust-loop-intro.mp4   (~30s, H.264 inside MP4 — works everywhere)

Place your AI-generated export here with that exact name. The page loads:
  /videos/trust-loop-intro.mp4

Optional (add later in LandingView if you want a custom first frame):
  trust-loop-poster.jpg  — then add poster="/videos/trust-loop-poster.jpg" on the <video> tag.

Tip: keep bitrate modest for mobile (e.g. 720p / ~2–5 Mbps) so the hero stays fast.
